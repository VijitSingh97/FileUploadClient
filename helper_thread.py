


class HelperThread():
    
    # run every 5 secs
    def sync():
        #
        # check files in /data and put time stamp in list
        # GET /status -> return list of files and time stamps
        # compare on client
        # sync old files with /download and upload new files with /upload